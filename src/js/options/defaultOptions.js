const defaultSpicrOptions = {
  delay: 250,
  duration: 500,
  easing: 'easingCubicOut',
  interval: 5000,
  touch: true,
  pause: 'hover',
  slides: {
    itemsPerPage: 3,
    totalRealItems: 5,
    activeAlign: 'center',
    gap: 8,
    axis: 'x',
  },
};

export default defaultSpicrOptions;
